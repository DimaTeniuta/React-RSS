export interface FormData {
  name: string;
  surname: string;
  birthday: string;
  country: string;
  avatar: string;
  personalData?: boolean;
  genderMale?: boolean;
}

export interface SelectData {
  value: string;
  name: string;
}
